# LangChain & AI (updated versions)
langchain==0.1.20
langchain-community==0.0.38
langchain-chroma==0.1.1

# Vector Database
chromadb==0.4.24
# chromadb 0.4.24 requires only posthog>=2.4.0, but posthog 4+ changed
# capture() to keyword-only args, so every startup logged
# "Failed to send telemetry event ... capture() takes 1 positional argument".
posthog<4

# Document Processing
pypdf==4.2.0
charset-normalizer==3.3.2
# Was in the original requirements and dropped in Stage 2 because nothing
# imported it. Back now that the loader actually reads .docx files.
python-docx==1.1.0

# API
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# Frontend
streamlit==1.34.0
requests==2.32.3

# Utils
python-dotenv==1.0.1
pydantic==2.7.1
pydantic-settings==2.2.1

# OpenAI (compatible version)
openai==1.30.1

# Telegram Bot
python-telegram-bot==21.1
httpx==0.27.0
